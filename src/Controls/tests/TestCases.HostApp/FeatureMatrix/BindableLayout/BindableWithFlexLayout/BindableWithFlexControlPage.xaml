<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Maui.Controls.Sample"
             x:Class="Maui.Controls.Sample.BindableWithFlexControlPage"
             x:DataType="local:BindableLayoutViewModel"
             Title="BindableLayoutControlPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Options"
                     Clicked="NavigateToOptionsPage_Clicked"/>
    </ContentPage.ToolbarItems>
    <VerticalStackLayout>
        <Label Text="BindableLayout FlexLayout"
               FontAttributes="Bold"
               FontSize="12"
               Margin="0,10,0,0"/>
        <FlexLayout 
                    BindableLayout.ItemsSource="{Binding ItemsSource}"
                    BindableLayout.ItemTemplate="{Binding ItemTemplate}"
                    BindableLayout.EmptyView="{Binding EmptyView}"
                    BindableLayout.EmptyViewTemplate="{Binding EmptyViewTemplate}"
                    AutomationId="BindableLayoutFlexLayout">
        </FlexLayout>
    </VerticalStackLayout>
</ContentPage>